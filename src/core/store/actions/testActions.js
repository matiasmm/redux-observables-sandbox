import reducer from '../reducer/testReducer'

export const { 
    increment, 
    initAsync, 
    asyncSuccess, 
    stopAsync, 
    initDepAsync1, 
    initDepAsync2,
    initDepAsync3,
    allDependenciesCompleted
} = reducer.actions